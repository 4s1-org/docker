stages:
  - deploy-prod
  - deploy-dev

deploy-node-14-alpine-prod-job:
  stage: deploy-prod
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: ['']
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
  variables:
    NODE_VERSION: 14-alpine
  script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"auth\":\"$(echo -n ${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD} | base64)\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor
      --context $CI_PROJECT_DIR
      --dockerfile $CI_PROJECT_DIR/node/alpine/prod/Dockerfile
      --build-arg NODE_VERSION=$NODE_VERSION
      --destination $CI_REGISTRY_IMAGE:$NODE_VERSION

deploy-node-14-buster-prod-job:
  stage: deploy-prod
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: ['']
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
  variables:
    NODE_VERSION: 14-buster
  script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"auth\":\"$(echo -n ${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD} | base64)\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor
      --context $CI_PROJECT_DIR
      --dockerfile $CI_PROJECT_DIR/node/buster/prod/Dockerfile
      --build-arg NODE_VERSION=$NODE_VERSION
      --destination $CI_REGISTRY_IMAGE:$NODE_VERSION

deploy-node-14-alpine-dev-job:
  stage: deploy-dev
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: ['']
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
  variables:
    NODE_VERSION: 14-alpine
  script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"auth\":\"$(echo -n ${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD} | base64)\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor
      --context $CI_PROJECT_DIR
      --dockerfile $CI_PROJECT_DIR/node/alpine/dev/Dockerfile
      --build-arg NODE_VERSION=$NODE_VERSION
      --destination $CI_REGISTRY_IMAGE:$NODE_VERSION-dev

deploy-node-14-buster-dev-job:
  stage: deploy-dev
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: ['']
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
  variables:
    NODE_VERSION: 14-buster
  script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"auth\":\"$(echo -n ${CI_REGISTRY_USER}:${CI_REGISTRY_PASSWORD} | base64)\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor
      --context $CI_PROJECT_DIR
      --dockerfile $CI_PROJECT_DIR/node/buster/dev/Dockerfile
      --build-arg NODE_VERSION=$NODE_VERSION
      --destination $CI_REGISTRY_IMAGE:$NODE_VERSION-dev
